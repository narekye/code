export module Classes {
    class User {
        private firstname: string;
        private lastname: string;

        constructor(f: string, l:string){
            this.firstname = f;
            this.lastname = f;
        }
    }
}