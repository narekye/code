#include <iostream>
#include <string>
using namespace std;

int main() {

	char arr[100];
	std::cout << "Please insert your calculatable expression";
	std::cin >> arr;
	std::cout << "Begin analyze";
	for (int i = 0; i < strlen(arr); i++) {
		switch (arr[i])
		{
		default:
			break;
		}
	}
	return 0;
}